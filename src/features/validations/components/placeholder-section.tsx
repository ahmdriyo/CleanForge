import { PlaceholderCard } from "@/components/ui/placeholder-card";
import { ShieldCheck } from "lucide-react";
export const ValidationsPlaceholder = () => (
  <PlaceholderCard icon={ShieldCheck} title="Validations" description="Validate your project structure against your clean standards." specific="Validations engine is under construction. We'll check kebab-case and folder rules automatically." />
);
