import { PlaceholderCard } from "@/components/ui/placeholder-card";
import { Settings } from "lucide-react";
export const SettingsPlaceholder = () => (
  <PlaceholderCard icon={Settings} title="Settings" description="Manage your profile, secrets, and preferences. Help & Feedback included." specific="Settings is under construction. You'll manage Firebase profile and Secret Manager here." />
);
